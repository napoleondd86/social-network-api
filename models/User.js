const { Schema, Types } = require("mongoose");
const { Thought } = require("./index")

const userSchema = new Schema (
  {
    username: {
      type: String,
      required: true,
      unique: true,
      trim: true, //  I THINK THIS WILL WORK ??????????????????????
    },
    email: {
      type: String,
      unique: true,
      validate: {
        validator: function(v) {
          return /^([a-z0-9_\.-]+)@([\da-z\.-]+)\.([a-z\.]{2,6})$/.test(v) // DOES THIS WORK ????????????????????????????
        },
        message: "Please provide a valid email!"
      },
      required: true,
    },
    thoughts: [
      {
        type: Schema.Types.ObjectId,
        ref: "Thought"
      }
    ],// DOES THIS WORK ///////?????????????????????????????????????
    friends: [
      {
        type: Schema.Types.ObjectId,
        ref: "User"
      }
    ],// DOES THIS WORK ///////?????????????????????????????????????

  },
  {
    timestamps: true,
    toJSON: {

    },
    id: false,

  }
)

